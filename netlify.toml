[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# The signal engine runs on a schedule (the "constantly reads it" part).
# Plain scheduled function (30s budget) — runs all pairs in parallel with fast
# Gemini Flash, well inside the limit. (Background functions can't be scheduled
# on Netlify and need a paid plan, so this is the correct free setup.)
# Runs every 30 minutes. "@hourly" or "*/15 * * * *" also work.
[functions."signal-engine"]
  schedule = "*/30 * * * *"

# The agent's morning deep-think: distill journal lessons into playbook
# principles + write the day plan. Weekdays 06:00 UTC (before London open).
[functions."daily-think"]
  schedule = "0 6 * * 1-5"

# The agent's fund-manager letter: live week vs fresh backtest benchmark +
# drift check. Fridays 21:30 UTC (after the market close).
[functions."weekly-letter"]
  schedule = "30 21 * * 5"

# Always revalidate the app shell — a cached index.html (or the installed PWA)
# must never pin an old deploy. Assets are tiny; ETag revalidation keeps it cheap.
[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "no-cache"
